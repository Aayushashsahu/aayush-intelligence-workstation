import type { LabEntry } from '@/lib/types'

/**
 * LAB — experiments and research threads.
 * ----------------------------------------------------------------------------
 * Initial public state is empty: NO PUBLISHED RESEARCH INDEXED.
 * Experiments will be published manually by the owner via /control.
 */

export const lab: LabEntry[] = []
