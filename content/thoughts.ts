import type { Thought } from '@/lib/types'

/**
 * CURATED THINKING / NOTES
 * ----------------------------------------------------------------------------
 * Deliberately empty. The workstation reserves space for notes and short
 * essays so the portfolio reads as a body of thinking, not only a build log.
 *
 * Add entries here (or from /control) — the LAB and FOUNDER surfaces will
 * pick them up automatically. Nothing is invented on your behalf.
 *
 * Example shape:
 * {
 *   id: 'agent-memory',
 *   title: 'What agent memory actually needs',
 *   body: '...',
 *   date: null,           // only set a date you are sure about
 *   topic: 'AI MEMORY',
 *   tags: ['agents', 'memory'],
 * }
 */
export const thoughts: Thought[] = []
